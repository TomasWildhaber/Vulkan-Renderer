#pragma once

#include <iostream>
#include <memory>
#include <utility>
#include <algorithm>
#include <functional>
#include <random>
#include <xhash>
#include <optional>

#include <string>
#include <sstream>
#include <array>
#include <vector>
#include <unordered_map>
#include <unordered_set>
#include <set>

#include <fstream>

#include <ctime>
#include <any>

#include <cstdint>

// TODO: fix
//#if (defined(__clang__) && (!defined(__has_builtin) || !__has_builtin(__builtin_debugtrap))) || (defined(__GNUC__) && !(defined(__i386__) || defined(__x86_64__))))
//#include <signal.h>
//#endif